  {{#if blogPosts.comments.length}}
  <div class="col-md-6 blogPost-list">
    <h3>Comments:</h3>

    <div class="text-center">
      <p>{{description}}</p>
      <p>Posted by {{user.name}} on {{format_date date_created}}</p>
    </div>
  </div>
  {{/if}}